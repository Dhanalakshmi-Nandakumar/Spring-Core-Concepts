# Lombok configuration for project
# Stop searching parent directories for other lombok.config files
config.stopBubbling = true

# Add the @lombok.Generated annotation to Lombok-generated members
lombok.addLombokGeneratedAnnotation = true

# Recommended: enable annotation processing in IDE/build if needed
# (IDE-specific plugin required for editor support)
